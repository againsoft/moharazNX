[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
# Do not run DB scripts in preDeploy — private network (postgres.railway.internal) is unavailable there.
