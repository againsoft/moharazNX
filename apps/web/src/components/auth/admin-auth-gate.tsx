"use client";

// AUTH TEMPORARILY DISABLED — remove this comment and restore the original to re-enable
export function AdminAuthGate({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
