import { SearchView } from "@/components/storefront/search/search-view";

export const metadata = {
  title: "Search — MoharazNX",
};

export default function SearchPage() {
  return <SearchView />;
}
