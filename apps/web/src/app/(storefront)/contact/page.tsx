import { ContactView } from "@/components/storefront/contact/contact-view";

export const metadata = {
  title: "Contact Us — MoharazNX",
  description: "Get in touch with MoharazNX support for orders, returns, and product questions.",
};

export default function ContactPage() {
  return <ContactView />;
}
