import { ShippingReturnsView } from "@/components/storefront/shipping/shipping-returns-view";

export const metadata = {
  title: "Shipping & Returns — MoharazNX",
  description: "Delivery options, shipping rates, and return policy for MoharazNX orders.",
};

export default function ShippingPage() {
  return <ShippingReturnsView />;
}
