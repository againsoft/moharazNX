import { AccountReturnsView } from "@/components/storefront/account/account-returns-view";

export const metadata = {
  title: "Returns — My account — MoharazNX",
  robots: { index: false, follow: false },
};

export default function AccountReturnsPage() {
  return <AccountReturnsView />;
}
