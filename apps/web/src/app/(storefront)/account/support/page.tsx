import { AccountSupportView } from "@/components/storefront/account/account-support-view";

export const metadata = {
  title: "Support — My account — MoharazNX",
  robots: { index: false, follow: false },
};

export default function AccountSupportPage() {
  return <AccountSupportView />;
}
